Placeholder {
  width: 100%;
  height: 30;
  layout: spread;

  background-color: rgba(10, 10, 10, 10);

  .identifier {
    margin-left: 3;
  }

  .use-button {
    width: 30;
    margin-right: 5;
  }
}